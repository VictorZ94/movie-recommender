{
  "name": "movie-recommender",
  "version": "1.0.0",
  "description": "Use settings.py for production. In the same directory create settings_dev.py for overrides.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tw:build": "npx tailwindcss -i ./static/src/input.css -o ./static/src/output.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier-plugin-tailwindcss": "^0.4.1",
    "tailwindcss": "^3.3.3"
  },
  "dependencies": {
    "flowbite": "^1.7.0"
  }
}
